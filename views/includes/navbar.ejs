<nav class="navbar navbar-expand-lg sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings" aria-label="Roamly home">
      <span class="brand-mark"><i class="fa-solid fa-compass"></i></span>
      <span class="brand-copy">Roamly<small>Stay curious</small></span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fa-solid fa-bars"></i>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-lg-3">
        <a class="nav-link" href="/listings"><i class="fa-regular fa-compass me-1"></i> Discover</a>
      </div>

      <form class="nav-search mx-lg-auto" action="/listings" method="GET" role="search">
        <input class="form-control search-inp" name="search" value="<%= typeof search !== 'undefined' ? search : '' %>" type="search" placeholder="Where are you going?" aria-label="Search stays" />
        <button class="btn search-btn" type="submit"><i class="fa-solid fa-arrow-right"></i></button>
      </form>

      <div class="nav-actions ms-lg-auto">
        <button class="theme-toggle" id="themeToggle" type="button" aria-label="Switch theme" title="Switch theme">
          <i class="fa-solid fa-moon"></i>
        </button>
        <% if (currUser) { %>
          <a class="nav-link" href="/listings/new"><i class="fa-solid fa-plus me-1"></i> Host a stay</a>
          <a class="nav-link" href="/profile"><i class="fa-regular fa-user me-1"></i> <%= currUser.username %></a>
          <form method="POST" action="/logout"><input type="hidden" name="_csrf" value="<%= csrfToken %>" /><button class="nav-link border-0 bg-transparent" type="submit">Sign out</button></form>
        <% } else { %>
          <a class="nav-link" href="/login">Log in</a>
          <a class="btn btn-dark ms-1" href="/signup">Join Roamly</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
